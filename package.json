{
  "name": "demo-sls",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "sls offline start --port 8080",
    "deploy": "sls deploy",
    "test": "jest",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md  -s && git add CHANGELOG.md",
    "change-help": "conventional-changelog --help",
    "eslint": "echo 'lint'"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.2.0",
    "@commitlint/config-conventional": "^8.2.0",
    "commitlint": "^8.2.0",
    "conventional-changelog-cli": "^2.0.25",
    "husky": "^3.0.8",
    "jest": "^24.9.0",
    "serverless-offline": "^5.11.0",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "koa": "^2.8.2",
    "koa-router": "^7.4.0",
    "koa-static": "^5.0.0",
    "node-fetch": "^2.6.0",
    "serverless-http": "^2.3.0"
  }
}
